{
  "name": "bt-mvp-web",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "lint": "next lint",
    "test": "vitest run",
    "check:contracts": "node scripts/check-contract-drift.mjs",
    "check:docs": "node scripts/check-doc-boundary.mjs",
    "verify": "npm run check:contracts && npm run check:docs && npm run lint && npm run test && npm run build && npm --prefix server run build"
  },
  "dependencies": {
    "next": "^14.2.35",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^18.2.55",
    "@types/react-dom": "^18.2.19",
    "eslint": "^8.57.1",
    "eslint-config-next": "14.2.35",
    "jsdom": "^27.4.0",
    "typescript": "^5.7.2",
    "vitest": "^4.0.18"
  }
}
